#include <catch2/catch_test_macros.hpp>
#include "modes/reference_mode.hpp"

TEST_CASE("Reference mode API compiles", "[reference]") {
    // Basic compilation test - full test requires test files
    REQUIRE(true);
}

TEST_CASE("Natural sort ordering", "[reference]") {
    // Test that tracks would be sorted correctly
    // (Implementation detail - API test)
    REQUIRE(true);
}